using UnityEngine;

namespace ItemsManager.Databases
{
    /// <summary>
    /// The main database, containing references to all other databases
    /// Those can be either built-in or separate So's
    /// </summary>
    public abstract class AbstractGeneralDatabase : ScriptableObject
    {
        
    }
}